using System;

class Program
{
    static void Main(string[] args)
    {
        // If no input but not scripture fully redactedm (get fully redactedm ) rldr rnf
        // If times redacted = word count, set scripture fully redacted boolean to true
    }
}